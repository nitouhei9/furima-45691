default: &default
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  adapter: sqlite3
  username: root
  password:
  host: localhost
  database: furima_445691_development

test:
  <<: *default
  adapter: sqlite3
  username: root
  password:
  host: localhost
  database: furima_445691_test

production:
  <<: *default
  adapter: mysql2
  database: furima_45691_production
  username: root
  password: <%= ENV['DATABASE_PASSWORD'] %>
  socket: /var/lib/mysql/mysql.sock